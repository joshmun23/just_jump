<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>



    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Oswald' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <div class="fixed">
      <nav class="top-bar">
        <section class="top-bar-section">
          <!-- Right Nav Section -->
          <ul class="right">

            <li class="top-bar-alert"><%= link_to "Home", root_path %></li>
            <li class="has-dropdown">
              <a href="#">Insert User Functionality</a>
              <ul class="dropdown">
                <li><a href="#">First link in dropdown</a></li>
                <li><a href="#">Active link in dropdown</a></li>
              </ul>
            </li>
          </ul>

          <!-- Left Nav Section -->
          <ul class="left">
            <li class="name">
              <%= link_to "Jump!", root_path %>
            </li>

            <li class="has-form">
              <div class="row collapse">

                <% if params[:food_search] %>
                  <%= form_for @spot, url: url_for(controller: 'food_searches', action: 'create') do |f| %>

                    <div class="large-5 small-9 columns">
                      <%= f.text_field :search_term, class: 'form-control', autofocus: true, placeholder: "What're you craving today?" %>
                    </div>
                    <div class="large-5 small-9 columns">
                      <%= f.text_field :search_location, class: 'form-control', autofocus: true, placeholder: 'And where are you?' %>
                    </div>
                    <div class="large-2 small-3 columns">
                      <%= f.submit "Feed Your Need", class: 'button small alert' %>
                    </div>

                  <% end %>
                <% end %>
              </div>
            </li>

          </ul>
        </section>
      </nav>

    </div>

    <div class="main-body">
      <div class="main-heading">
        <h1 class="main-title">Just Jump!</h1>
      </div>

      <div class="main-content">
        <ul>
          <% if params[:controller] == 'food_searches' && !params[:food_search]%>
            <li class="has-form">
              <div class="row collapse">
                  <%= form_for @spot, url: url_for(controller: 'food_searches', action: 'create') do |f| %>

                    <div class="large-6 small-9 columns">
                      <%= f.text_field :search_term, class: 'form-control', autofocus: true, placeholder: "What're you craving today?" %>
                    </div>
                    <div class="large-6 small-9 columns">
                      <%= f.text_field :search_location, class: 'form-control', autofocus: true, placeholder: 'And where are you?' %>
                    </div>
                    <div class="large-12 small-3 columns">
                      <%= f.submit "Feed Your Need", class: 'button small alert' %>
                    </div>

                  <% end %>
              </div>
            </li>
          <% end %>
        </ul>

        <%= yield %>
      <div>
    </div>
  </body>
</html>
