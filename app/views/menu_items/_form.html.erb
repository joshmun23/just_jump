<%= form_tag(controller:'menu_items', action: 'index',
  method: 'post') %>
  <% #insert error checking here %>

  <%= m.label :name %>
  <%= m.text_field :name %>

  <%= m.label :price %>
  <%= m.text_field :price %>

  <%= m.submit %>
<% end %>

<%= form_for(@menu_item) do |f| %>
  <%= f.label :find_your_food %>
  <%= f.search_field :name %>

  <%= f.submit %>

<% end %>
